.env
*.pyc
__pycache__/
.venv/
*.db

node_modules/
dist/
dist-ssr/
*.local

.DS_Store
*.tsbuildinfo
*.egg-info/
.pytest_cache/
.ruff_cache/

# PyInstaller intermediates (agent/build/ is source; agent/.pyinstaller/ is scratch)
.pyinstaller/

# Trained novelty models: generated artifacts, rebuilt from TimescaleDB by
# backend/scripts/train_novelty_model.py. Never source, never in git.
backend/var/

# Test run output. The suites and the reports that render them are source; a
# particular run's results are not — they describe one machine at one moment,
# and a committed .xlsx is a stale claim about a pass rate.
selenium-tests/results.json
appium-tests/results.json
load-tests/summary.json
*.xlsx
