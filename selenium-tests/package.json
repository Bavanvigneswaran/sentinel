{
  "name": "sentinel-selenium-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Selenium E2E suite for the Sentinel web console.",
  "scripts": {
    "test": "mocha tests/*.js --timeout 60000 --reporter spec",
    "test:report": "mocha tests/*.js --timeout 60000 --reporter json --reporter-option output=results.json || true",
    "report": "TEST_SUITE_NAME='Sentinel web console — Selenium E2E' node ../tools/test-report/mocha-xlsx.js results.json selenium-test-report.xlsx",
    "test:xlsx": "npm run test:report && npm run report",
    "smoke": "node smoke.js"
  },
  "dependencies": {
    "selenium-webdriver": "^4.27.0"
  },
  "devDependencies": {
    "exceljs": "^4.4.0",
    "mocha": "^11.0.1"
  }
}
